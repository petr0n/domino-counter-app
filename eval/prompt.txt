This photo shows one or more domino tiles from a DOUBLE-12 set. Each tile is a
rectangle split by a center bar into two halves. Each half shows 0–12 pips
(raised circular dots). The image is grayscale — some pips appear as very light
gray circles on the white tile. Count ALL pip dots, dark or light.

Tiles can be at any angle. The dividing bar can be horizontal or vertical.

FOR EACH TILE:
1. Find the center bar and identify the two halves.
2. For each half, count row by row (top to bottom):
   "row 1 = N pips, row 2 = N pips, ..." then total.
3. Then confirm with column count:
   - 2 columns wide → value ∈ {2,4,6,8,10}
   - 3 columns wide → value ∈ {9,12}
   - 9 is a SQUARE 3×3 block. 6 is a NARROWER 2×3 block (only 2 columns wide).

Write one line per tile showing row counts and the final values, then end with:
{"tiles":[{"left":<0-12>,"right":<0-12>}, ...]}
